FROM node:20-slim

WORKDIR /app

# Copy package files from the current directory (saas-cloud)
COPY package*.json ./
RUN npm install

# Copy everything else from saas-cloud
COPY . .

EXPOSE 3000

CMD ["node", "server/cloud_controller.js"]